<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://kit.fontawesome.com/ff444f3eca.js" crossorigin="anonymous"></script>
</head>
<body class="bg-zinc-900 text-white">

    <div class="flex items-center justify-center min-h-screen">

        <!-- Main Content -->
        <div class="w-full max-w-md bg-zinc-800 p-8 rounded-lg shadow-lg">
            <!-- Go back button with FontAwesome back icon -->
            <a href="/" class="text-blue-400 text-sm mb-4 inline-block hover:underline">
                <i class="fas fa-arrow-left mr-2"></i> Home Page
            </a>

            <h1 class="text-2xl font-semibold mb-6">Edit Task</h1>

            <!-- Edit Form -->
            <form action="/edit" method="post">
                <!-- Previous Name Input -->
                <div class="mb-5">
                    <label for="previous" class="block text-sm text-zinc-400 mb-1">Previous Name</label>
                    <input 
                        type="text" 
                        name="previous" 
                        id="previous" 
                        class="block w-full p-3 rounded-md bg-zinc-700 text-white focus:ring-2 focus:ring-blue-500"
                        value="<%= filename %>" 
                        placeholder="Previous name" 
                        required
                    >
                </div>

                <!-- New Name Input -->
                <div class="mb-5">
                    <label for="new" class="block text-sm text-zinc-400 mb-1">New Name (Optional)</label>
                    <input 
                        type="text" 
                        name="new" 
                        id="new" 
                        class="block w-full p-3 rounded-md bg-zinc-700 text-white focus:ring-2 focus:ring-blue-500"
                        placeholder="Updated name (leave blank if unchanged)"
                    >
                </div>

                <!-- Description Input -->
                <div class="mb-5">
                    <label for="description" class="block text-sm text-zinc-400 mb-1">Description (Optional)</label>
                    <textarea 
                        name="description" 
                        id="description" 
                        class="block w-full p-3 rounded-md bg-zinc-700 text-white focus:ring-2 focus:ring-blue-500"
                        placeholder="Updated description (leave blank if unchanged)"
                    ><%= description %></textarea>
                </div>

                <button 
                    type="submit" 
                    class="w-full bg-blue-500 py-3 rounded-md hover:bg-blue-700 transition duration-200"
                >
                    Update Task
                </button>
            </form>
        </div>

    </div>

</body>
</html>
